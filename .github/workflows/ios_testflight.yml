name: iOS - Build IPA + Upload TestFlight (NO dart-define)

on:
  workflow_dispatch:

jobs:
  ios:
    runs-on: macos-14

    env:
      IOS_P12_B64: ${{ secrets.IOS_P12_B64 }}
      IOS_P12_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
      IOS_MOBILEPROVISION_B64: ${{ secrets.IOS_MOBILEPROVISION_B64 }}
      IOS_TEAM_ID: ${{ secrets.IOS_TEAM_ID }}
      IOS_EXPORT_METHOD: ${{ secrets.IOS_EXPORT_METHOD }} # app-store
      IOS_CODE_SIGN_IDENTITY: ${{ secrets.IOS_CODE_SIGN_IDENTITY }} # Apple Distribution

      ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
      ASC_KEY_P8_B64: ${{ secrets.ASC_KEY_P8_B64 }}

      COCOAPODS_DISABLE_STATS: "true"
      FASTLANE_OPT_OUT_USAGE: "true"
      LANG: en_US.UTF-8
      LC_ALL: en_US.UTF-8

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.32.0"
          channel: stable
          cache: true

      - name: Ensure iOS platform exists (generate ios/)
        run: |
          flutter clean
          flutter create --platforms=ios .
          flutter pub get

      - name: Set minimum iOS deployment target
        run: |
          # Uncomment and set platform in Podfile to iOS 14.0
          if grep -q "# platform :ios" ios/Podfile; then
            sed -i '' "s/# platform :ios, '.*'/platform :ios, '14.0'/" ios/Podfile
          elif ! grep -q "platform :ios" ios/Podfile; then
            sed -i '' "1s/^/platform :ios, '14.0'\n/" ios/Podfile
          else
            sed -i '' "s/platform :ios, '.*'/platform :ios, '14.0'/" ios/Podfile
          fi
          
          # Add post_install hook to set deployment target for all pods
          if ! grep -q "post_install do" ios/Podfile; then
            cat >> ios/Podfile <<'EOF'

          post_install do |installer|
            installer.pods_project.targets.each do |target|
              target.build_configurations.each do |config|
                config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '14.0'
              end
            end
          end
          EOF
                    fi
          
          # Update Xcode project deployment target
          sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = [0-9.]*;/IPHONEOS_DEPLOYMENT_TARGET = 14.0;/g' ios/Runner.xcodeproj/project.pbxproj
          
          echo "✅ iOS deployment target set to 14.0"

      - name: Check Podfile exists
        run: |
          ls -la ios
          echo "--- Podfile content (first 30 lines) ---"
          cat ios/Podfile | head -30
          test -f ios/Podfile && echo "✅ Podfile found" || (echo "❌ Podfile missing" && exit 1)

      - name: Debug P12 decode
        shell: bash
        run: |
          set -euo pipefail
          echo "$IOS_P12_B64" | tr -d '\n\r ' | base64 --decode > "$RUNNER_TEMP/cert.p12"
          echo "Size:"
          ls -lh "$RUNNER_TEMP/cert.p12"
          echo "File type:"
          file "$RUNNER_TEMP/cert.p12" || true
          echo "Try open with openssl (validates password too):"
          openssl pkcs12 -in "$RUNNER_TEMP/cert.p12" -passin pass:"$IOS_P12_PASSWORD" -info -noout
          echo "✅ P12 valid + password OK"

      - name: Install signing cert + provisioning profile
        shell: bash
        run: |
          set -euo pipefail

          KEYCHAIN_PATH="$RUNNER_TEMP/build.keychain"
          KEYCHAIN_PWD="ci_pwd_123"

          # Create and configure keychain
          security create-keychain -p "$KEYCHAIN_PWD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PWD" "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH" login.keychain-db
          security default-keychain -s "$KEYCHAIN_PATH"

          # Import certificate
          echo "$IOS_P12_B64" | tr -d '\n\r ' | base64 --decode > "$RUNNER_TEMP/cert.p12"
          security import "$RUNNER_TEMP/cert.p12" -k "$KEYCHAIN_PATH" -P "$IOS_P12_PASSWORD" -A \
            -T /usr/bin/codesign -T /usr/bin/security -T /usr/bin/xcodebuild

          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PWD" "$KEYCHAIN_PATH"

          # Install provisioning profile
          PP_DIR="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$PP_DIR"
          
          # Decode and save provisioning profile to temp location first
          echo "$IOS_MOBILEPROVISION_B64" | tr -d '\n\r ' | base64 --decode > "$RUNNER_TEMP/profile.mobileprovision"
          
          # Verify the provisioning profile file exists and has content
          if [ ! -s "$RUNNER_TEMP/profile.mobileprovision" ]; then
            echo "❌ Provisioning profile is empty or missing"
            exit 1
          fi
          
          # Extract profile info to a temporary plist
          security cms -D -i "$RUNNER_TEMP/profile.mobileprovision" > "$RUNNER_TEMP/profile.plist"
          
          # Extract UUID from the plist
          PP_UUID=$(/usr/libexec/PlistBuddy -c "Print :UUID" "$RUNNER_TEMP/profile.plist")
          PP_NAME=$(/usr/libexec/PlistBuddy -c "Print :Name" "$RUNNER_TEMP/profile.plist")
          APP_ID=$(/usr/libexec/PlistBuddy -c "Print :Entitlements:application-identifier" "$RUNNER_TEMP/profile.plist" 2>/dev/null || echo "N/A")
          TEAM_ID=$(/usr/libexec/PlistBuddy -c "Print :TeamIdentifier:0" "$RUNNER_TEMP/profile.plist" 2>/dev/null || echo "N/A")
          
          # Copy to the correct location with UUID as filename
          cp "$RUNNER_TEMP/profile.mobileprovision" "$PP_DIR/$PP_UUID.mobileprovision"
          
          # Store for later use
          echo "$PP_UUID" > "$RUNNER_TEMP/pp_uuid.txt"
          echo "$PP_NAME" > "$RUNNER_TEMP/pp_name.txt"

          echo "✅ Provisioning Profile installed"
          echo "  UUID: $PP_UUID"
          echo "  Name: $PP_NAME"
          echo "  App ID: $APP_ID"
          echo "  Team ID: $TEAM_ID"
          
          echo ""
          echo "=== Installed Provisioning Profiles ==="
          ls -la "$PP_DIR"
          
          echo ""
          echo "=== Available Code Signing Identities ==="
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"
          
          echo ""
          echo "✅ Setup complete"

      - name: Configure Xcode project for manual signing
        shell: bash
        run: |
          set -euo pipefail
          
          # Read the stored values
          PP_UUID=$(cat "$RUNNER_TEMP/pp_uuid.txt")
          PP_NAME=$(cat "$RUNNER_TEMP/pp_name.txt")
          
          # Get bundle ID from Info.plist
          BUNDLE_ID=$(/usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" ios/Runner/Info.plist)
          
          echo "=== Configuration Values ==="
          echo "Team ID: $IOS_TEAM_ID"
          echo "Profile UUID: $PP_UUID"
          echo "Profile Name: $PP_NAME"
          echo "Code Sign Identity: $IOS_CODE_SIGN_IDENTITY"
          echo "Bundle ID: $BUNDLE_ID"
          
          PBXPROJ="ios/Runner.xcodeproj/project.pbxproj"
          
          # Backup
          cp "$PBXPROJ" "$PBXPROJ.backup"
          
          # Remove Xcode's workspace data that caches signing settings
          rm -rf ios/Runner.xcodeproj/project.xcworkspace/xcuserdata || true
          rm -rf ios/Runner.xcodeproj/xcuserdata || true
          rm -rf ios/Runner.xcworkspace/xcuserdata || true
          
          echo ""
          echo "=== Modifying project.pbxproj ==="
          
          # CRITICAL: Change ProvisioningStyle from Automatic to Manual in TargetAttributes
          # This is what tells Xcode not to use automatic provisioning
          sed -i '' 's/ProvisioningStyle = Automatic;/ProvisioningStyle = Manual;/g' "$PBXPROJ"
          
          # Also remove any DevelopmentTeam from TargetAttributes that might override
          sed -i '' '/DevelopmentTeam = .*;/d' "$PBXPROJ"
          
          # Find all XCBuildConfiguration sections and identify which ones belong to Runner target
          # We'll use Python for more precise editing
          python3 <<PYTHON_SCRIPT
          import re
          import sys
          
          with open("$PBXPROJ", 'r') as f:
              content = f.read()
          
          # Find the Runner target configurations (Debug, Release, Profile)
          # These are typically named with comments like "/* Debug */ ="
          runner_config_pattern = r'(/\* (Debug|Release|Profile) \*/ = \{[^}]*?buildSettings = \{)(.*?)(\};)'
          
          def add_signing_settings(match):
          prefix = match.group(1)
          config_name = match.group(2)
          build_settings = match.group(3)
          suffix = match.group(4)
          
          # Check if this is likely a Runner target config (not Pods)
          # Runner configs usually have ASSETCATALOG_COMPILER or INFOPLIST_FILE
          if 'ASSETCATALOG_COMPILER' in build_settings or 'INFOPLIST_FILE' in build_settings or 'Runner/Info.plist' in build_settings:
              # Remove existing signing settings to avoid duplicates
              build_settings = re.sub(r'\s*CODE_SIGN_IDENTITY.*?;', '', build_settings)
            build_settings = re.sub(r'\s*CODE_SIGN_STYLE.*?;', '', build_settings)
            build_settings = re.sub(r'\s*DEVELOPMENT_TEAM.*?;', '', build_settings)
            build_settings = re.sub(r'\s*PROVISIONING_PROFILE_SPECIFIER.*?;', '', build_settings)
            build_settings = re.sub(r'\s*PROVISIONING_PROFILE[^_].*?;', '', build_settings)
            
            # Add our signing settings
            new_settings = f'''
            \t\t\t\tCODE_SIGN_IDENTITY = "$IOS_CODE_SIGN_IDENTITY";
            \t\t\t\t"CODE_SIGN_IDENTITY[sdk=iphoneos*]" = "$IOS_CODE_SIGN_IDENTITY";
            \t\t\t\tCODE_SIGN_STYLE = Manual;
            \t\t\t\tDEVELOPMENT_TEAM = $IOS_TEAM_ID;
            \t\t\t\tPROVISIONING_PROFILE_SPECIFIER = "$PP_UUID";'''
            
            build_settings = new_settings + build_settings
            
            print(f"✅ Modified {config_name} configuration", file=sys.stderr)
        
           return prefix + build_settings + suffix
    
          # Apply the modifications
          content = re.sub(runner_config_pattern, add_signing_settings, content, flags=re.DOTALL)
          
          with open("$PBXPROJ", 'w') as f:
              f.write(content)
          
          print("✅ Python modifications complete", file=sys.stderr)
          PYTHON_SCRIPT     
            
            echo "✅ Modified project.pbxproj"
          
          # Verify the changes
          echo ""
          echo "=== Verification ==="
          echo "Checking for Manual ProvisioningStyle:"
          grep "ProvisioningStyle = Manual" "$PBXPROJ" && echo "✅ Found" || echo "⚠️  Not found"
          
          echo ""
          echo "Checking for PROVISIONING_PROFILE_SPECIFIER with our UUID:"
          grep "PROVISIONING_PROFILE_SPECIFIER.*$PP_UUID" "$PBXPROJ" && echo "✅ Found" || echo "⚠️  Not found"
          
          echo ""
          echo "Checking for CODE_SIGN_STYLE = Manual:"
          grep "CODE_SIGN_STYLE = Manual" "$PBXPROJ" && echo "✅ Found" || echo "⚠️  Not found"
          
          echo ""
          echo "Checking for DEVELOPMENT_TEAM:"
          grep "DEVELOPMENT_TEAM = $IOS_TEAM_ID" "$PBXPROJ" && echo "✅ Found" || echo "⚠️  Not found"
          
          echo ""
          echo "✅ Configuration complete"

      - name: Update ExportOptions.plist
        shell: bash
        run: |
          set -euo pipefail
          
          # Get bundle ID from Info.plist
          BUNDLE_ID=$(/usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" ios/Runner/Info.plist)
          
          # Read stored provisioning profile info
          PP_NAME=$(cat "$RUNNER_TEMP/pp_name.txt")
          
          # Create ExportOptions.plist
          cat > ios/ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>${IOS_EXPORT_METHOD}</string>
              <key>teamID</key>
              <string>${IOS_TEAM_ID}</string>
              <key>signingStyle</key>
              <string>manual</string>
              <key>provisioningProfiles</key>
              <dict>
                  <key>${BUNDLE_ID}</key>
                  <string>${PP_NAME}</string>
              </dict>
              <key>signingCertificate</key>
              <string>${IOS_CODE_SIGN_IDENTITY}</string>
              <key>uploadSymbols</key>
              <true/>
              <key>compileBitcode</key>
              <false/>
          </dict>
          </plist>
          EOF
          
          echo "✅ Created ExportOptions.plist"
          echo "Bundle ID: $BUNDLE_ID"
          echo "Provisioning Profile Name: $PP_NAME"
          cat ios/ExportOptions.plist

      - name: Install CocoaPods
        run: |
          cd ios
          pod repo update
          pod install --repo-update
          cd ..

      - name: Build iOS Archive
        shell: bash
        run: |
          set -euo pipefail
          
          # Read provisioning profile UUID
          PP_UUID=$(cat "$RUNNER_TEMP/pp_uuid.txt")
          
          # Build the archive using xcodebuild directly for better control
          cd ios
          
          # Build archive - don't set PROVISIONING_PROFILE_SPECIFIER globally as it affects Pods
          # The provisioning profile is already configured in the project.pbxproj
          xcodebuild -workspace Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath "$RUNNER_TEMP/Runner.xcarchive" \
            -destination 'generic/platform=iOS' \
            archive
          
          echo "✅ Archive created"

      - name: Export IPA
        shell: bash
        run: |
          set -euo pipefail
          
          mkdir -p build/ios/ipa
          
          xcodebuild -exportArchive \
            -archivePath "$RUNNER_TEMP/Runner.xcarchive" \
            -exportPath build/ios/ipa \
            -exportOptionsPlist ios/ExportOptions.plist \
            -allowProvisioningUpdates
          
          echo "✅ IPA exported"
          ls -lh build/ios/ipa/

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: build/ios/ipa/*.ipa
          if-no-files-found: error

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"

      - name: Install fastlane
        run: gem install fastlane -N

      - name: Create ASC api_key.json
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .private
          echo "$ASC_KEY_P8_B64" | tr -d '\n\r ' | base64 --decode > .private/AuthKey.p8

          cat > .private/api_key.json <<EOF
          {
            "key_id": "${ASC_KEY_ID}",
            "issuer_id": "${ASC_ISSUER_ID}",
            "key_filepath": ".private/AuthKey.p8",
            "in_house": false
          }
          EOF

      - name: Upload to TestFlight
        shell: bash
        run: |
          set -euo pipefail

          IPA_PATH="$(ls -1 build/ios/ipa/*.ipa | head -n 1)"
          [ -f "$IPA_PATH" ] || { echo "❌ IPA not found"; exit 1; }

          TMP_DIR="$(mktemp -d)"
          unzip -q "$IPA_PATH" -d "$TMP_DIR"
          APP_PATH="$(find "$TMP_DIR/Payload" -maxdepth 1 -type d -name "*.app" | head -n 1)"
          BUNDLE_ID=$(/usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" "$APP_PATH/Info.plist")

          echo "✅ Uploading: $BUNDLE_ID"
          fastlane pilot upload \
            --ipa "$IPA_PATH" \
            --api_key_path ".private/api_key.json" \
            --app_identifier "$BUNDLE_ID" \
            --skip_waiting_for_build_processing true

          echo "✅ Uploaded. Check App Store Connect → TestFlight."
